<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hospital Registration - MediQR</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <style>
        /* CSS Variables */
        :root {
            --primary-purple: #4a00e0;
            --secondary-purple: #8e2de2;
            --text-dark: #333;
            --text-light: #888;
            --bg-white: #fff;
            --bg-light-gray: #f5f5f5;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Poppins', sans-serif; }
        body { display: flex; justify-content: center; align-items: center; min-height: 100vh; background-color: #f0f2f5; }

        /* Main Container and Layout */
        .registration-page {
            display: flex;
            width: 100%;
            max-width: 1200px;
            min-height: 700px;
            background-color: var(--bg-white);
            box-shadow: 0 15px 45px rgba(0, 0, 0, 0.15);
            border-radius: 25px;
            overflow: hidden;
        }

        /* Left Sidebar */
        .sidebar {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 50px;
            background: linear-gradient(135deg, var(--secondary-purple), var(--primary-purple));
            color: var(--bg-white);
        }
        .sidebar h1 { font-size: 3rem; font-weight: 800; margin-bottom: 20px; }
        .sidebar p { font-size: 1.1rem; line-height: 1.6; margin-bottom: 40px; }
        .sidebar .login-btn { 
            padding: 12px 40px; border: 2px solid var(--bg-white); border-radius: 30px; 
            background: transparent; color: var(--bg-white); font-size: 1rem; font-weight: 600; 
            text-decoration: none; transition: all 0.3s ease; 
        }
        .sidebar .login-btn:hover { background-color: var(--bg-white); color: var(--primary-purple); }

        /* Form Area */
        .form-area {
            flex: 1.2; 
            padding: 50px;
            display: flex;
            flex-direction: column;
            align-items: center; 
        }

        /* Tab Switcher (Specific to Patient/Hospital) */
        .tab-switcher {
            display: flex; width: 100%; max-width: 300px; background-color: #f0f0f0;
            border-radius: 30px; padding: 5px; margin-bottom: 40px;
        }
        .tab-switcher button {
            flex: 1; padding: 10px 0; border: none; border-radius: 30px;
            background-color: transparent; color: var(--text-dark); font-weight: 600; cursor: pointer;
            transition: all 0.3s ease;
        }
        .tab-switcher .hospital-tab { 
            background: var(--primary-purple); color: var(--bg-white); 
            box-shadow: 0 4px 10px rgba(74, 0, 224, 0.4);
        }

        .form-area h2 { font-size: 1.8rem; font-weight: 700; color: var(--text-dark); margin-bottom: 30px; align-self: flex-start; }

        /* Input Styling */
        .input-group { position: relative; width: 100%; max-width: 400px; margin-bottom: 20px; }
        .input-group input {
            width: 100%; padding: 15px 15px 15px 45px; border: none; background-color: var(--bg-light-gray);
            border-radius: 10px; font-size: 1rem; color: var(--text-dark); outline: none;
        }
        .input-group i {
            position: absolute; left: 15px; top: 50%; transform: translateY(-50%);
            color: var(--text-light); font-size: 1rem; pointer-events: none;
        }

        /* Hospital specific address row */
        .address-row { display: flex; gap: 10px; }
        .address-row .input-group { flex: 1; }
        
        /* File Upload Styling */
        .file-upload-box {
            padding: 20px 15px; text-align: left; border: 2px dashed #ddd;
            color: var(--text-light); cursor: pointer; transition: border-color 0.3s ease;
            position: relative; border-radius: 10px; margin-bottom: 20px;
        }
        .file-upload-box:hover { border-color: var(--primary-purple); }
        .file-upload-box input[type="file"] {
            position: absolute; left: 0; top: 0; width: 100%; height: 100%;
            opacity: 0; cursor: pointer;
        }
        .file-upload-label { font-size: 0.9rem; color: var(--text-light); }

        /* Doctor Verification Boxes */
        .doctor-verification-row { display: flex; gap: 20px; margin-bottom: 30px; }
        .doctor-verification-box {
            flex: 1; padding: 20px; border: 2px solid var(--bg-light-gray); border-radius: 10px;
            text-align: center; transition: all 0.3s ease; cursor: pointer;
        }
        .doctor-verification-box.active {
            border-color: var(--primary-purple); box-shadow: 0 0 10px rgba(74, 0, 224, 0.1);
        }
        .doctor-verification-box i { font-size: 2rem; color: var(--primary-purple); margin-bottom: 10px; }
        .doctor-verification-box p { font-weight: 600; color: var(--text-dark); }
        
        /* Submit Button */
        .submit-btn {
            display: block; width: 100%; max-width: 400px; padding: 15px; border: none;
            border-radius: 50px; background: linear-gradient(90deg, var(--secondary-purple), var(--primary-purple));
            color: var(--bg-white); font-size: 1.1rem; font-weight: 600; text-transform: uppercase;
            cursor: pointer; transition: all 0.3s ease; box-shadow: 0 5px 15px rgba(74, 0, 224, 0.4);
        }
        .submit-btn:hover { opacity: 0.9; transform: translateY(-2px); }

        .login-text { text-align: center; margin-top: 20px; font-size: 0.9rem; }
        .login-text a { color: var(--primary-purple); font-weight: 600; text-decoration: none; }
    </style>
</head>
<body>

<div class="registration-page">
    <div class="sidebar">
        <h1>Your Health in Your Hands</h1>
        <p>Securely manage and access your medical records anytime, anywhere. Take control of your healthcare journey today.</p>
        <a href="/login" class="login-btn">Login</a>
    </div>

    <div class="form-area">
        
        <div class="tab-switcher">
            <a href="/registerpatient" class=" regi-tab"><button>Patient</button></a>
            <button class="hospital-tab">Hospital</button>
        </div>

        <h2>CREATE HOSPITAL ACCOUNT</h2>

        <form action="/hospital/register" method="POST" enctype="multipart/form-data" id="hospital-registration-form" style="width: 100%; max-width: 400px;">
            
            <input type="hidden" name="doctorVerification" id="doctorVerificationInput" value="upload">
            
            <div class="input-group">
                <input type="text" name="hospitalName" placeholder="Hospital Name" required>
                <i class="fas fa-hospital"></i>
            </div>
            
            <div class="input-group">
                <input type="email" name="email" placeholder="Email" required>
                <i class="fas fa-envelope"></i>
            </div>
            
            <div class="input-group">
                <input type="password" name="password" placeholder="Password" required>
                <i class="fas fa-lock"></i>
            </div>

            <div class="address-row">
                <div class="input-group">
                    <input type="text" name="streetAddress" placeholder="Street Address" required>
                    <i class="fas fa-map-marker-alt"></i>
                </div>
                <div class="input-group">
                    <input type="text" name="cityStateZip" placeholder="City, State, ZIP" required>
                    <i class="fas fa-city"></i>
                </div>
            </div>

            <h4 style="color: var(--text-dark); margin-bottom: 10px; align-self: flex-start; font-weight: 600;">Verification Documents</h4>
            <div class="input-group" style="max-width: none;">
                <div class="file-upload-box">
                    Hospital License
                    <input type="file" name="hospitalLicense" accept=".pdf, .jpg, .png" required>
                    <p class="file-upload-label">Choose File</p>
                </div>
            </div>
            
            <h4 style="color: var(--text-dark); margin-bottom: 10px; align-self: flex-start; font-weight: 600;">Doctor Verification</h4>
            <div class="doctor-verification-row" id="doctor-verification-options" style="width: 100%; max-width: 400px;">
                <div class="doctor-verification-box" data-value="email">
                    <i class="fas fa-paper-plane"></i>
                    <p>Via Email</p>
                </div>
                <div class="doctor-verification-box active" data-value="upload">
                    <i class="fas fa-cloud-upload-alt"></i>
                    <p>Upload License</p>
                </div>
            </div>

            <button type="submit" class="submit-btn">Register</button>
        </form>
    </div>
</div>

<script>
    const form = document.getElementById('hospital-registration-form');
    const licenseInput = document.querySelector('input[name="hospitalLicense"]');
    const fileLabel = document.querySelector('.file-upload-label');
    const verificationInput = document.getElementById('doctorVerificationInput');
    const verificationOptions = document.getElementById('doctor-verification-options');

    // 1. Script for displaying the selected file name
    licenseInput.addEventListener('change', function() {
        if (this.files.length > 0) {
            fileLabel.textContent = this.files[0].name;
        } else {
            fileLabel.textContent = 'Choose File';
        }
    });

    // 2. Script for handling the Doctor Verification selection
    verificationOptions.addEventListener('click', function(e) {
        const box = e.target.closest('.doctor-verification-box');
        if (box) {
            // Remove 'active' from all boxes
            document.querySelectorAll('.doctor-verification-box').forEach(b => b.classList.remove('active'));
            
            // Add 'active' to the clicked box
            box.classList.add('active');
            
            // Update the hidden input value (sends to the backend)
            const verificationValue = box.getAttribute('data-value');
            verificationInput.value = verificationValue;

            // BUG FIX: Enable/Disable file input based on selection
            if (verificationValue === 'upload') {
                // If using 'upload', the file input should be enabled and required
                licenseInput.removeAttribute('disabled');
                licenseInput.setAttribute('required', 'required'); 
            } else {
                // If using 'email', the file input should be disabled and not required
                licenseInput.setAttribute('disabled', 'disabled');
                licenseInput.removeAttribute('required');
                licenseInput.value = null; // Clear any selected file
                fileLabel.textContent = 'Choose File'; // Reset label
            }
        }
    });

    // 3. Asynchronous Form Submission Logic (Handles the POST request)
    form.addEventListener('submit', async function(e) {
        e.preventDefault(); // Stop default form submission
        
        // FormData correctly handles multipart/form-data for files and text fields
        const formData = new FormData(form);

        try {
            const response = await fetch(form.action, {
                method: 'POST',
                // DO NOT set Content-Type header; the browser does it automatically for FormData with a boundary
                body: formData 
            });

            const result = await response.json();

            if (response.ok) {
                alert("✅ SUCCESS: Hospital registered! Verification is pending. Message: " + result.message);
                 window.location.href = '/dashboard';
                form.reset(); // Clear form on success
            } else {
                // Display validation errors or server messages
                let errorMessage = "Registration failed!";
                if (result.errors) {
                    errorMessage += "\n\nValidation Errors:\n" + result.errors.map(err => `• ${err.msg} (${err.param})`).join('\n');
                } else if (result.message) {
                    errorMessage += "\n\nServer Message: " + result.message;
                }
                alert("❌ ERROR: " + errorMessage);
            }
        } catch (error) {
            console.error('Network or unexpected error:', error);
            alert("⚠️ A critical network error occurred while contacting the server.");
        }
    });
</script>

</body>
</html>